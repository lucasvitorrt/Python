# Faça um programa que leia uma frase pelo teclado e mostre:
# Quantas vezes aparece a letra A.
# Em que posição ela aparece a primeira Vez.
# Em quem Posição ela aparece a ultima vez.
frase = str(input('Entre com uma frase qualquer:'))
frase = frase.upper()
print('Na frase a letra A aparece {} vezes!'.format(frase.count('A')))
print('A primeira posição em quem ela aparece é a posição: {}'.format(frase.find('A')))
print('A ultima posição em quem ela aparece é a posição: {}'.format(frase.rfind('A')))
